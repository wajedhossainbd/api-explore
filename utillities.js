const setInnertextByID = (id, text) =>{
    const data = document.getElementById(id);
    data.innerText = text;
    
}

const setValueByID = (id, text) =>{
    const data = document.getElementById('id');
    data.innerText = text;
    
}